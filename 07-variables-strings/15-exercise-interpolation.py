# Pring a sentence joining together the fruit and ripeness variables
# Use .format() method to interpolate strings
fruit = "banana"
ripeness = "unripe"

sentence = "The {} is {} to consume.".format(fruit, ripeness)
print(sentence)
