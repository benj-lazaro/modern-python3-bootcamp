# Write a lambda that accepts a single number
# Return its cubed value

cubed = lambda number: number ** 3

print("Lambda that accepts a single number and returns its cubed value.")
print(f"cubed(3): {cubed(3)}")
