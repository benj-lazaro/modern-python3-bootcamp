# Given the dictionary below:
artist = {
    "first": "Neil",
    "last": "Young",
}

# Declare a variable called full_name
# Access and assign the first & last dictionary keys into full_name as a single string with a space in-between
full_name = artist["first"] + " " + artist["last"]
print(full_name)
